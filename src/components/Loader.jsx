import Logo from './Logo';

const Loader = () => {

    return (
        <div className="center-content w-full h-[100vh] font-bold">
            <Logo />
        </div>
    );
}

export default Loader;